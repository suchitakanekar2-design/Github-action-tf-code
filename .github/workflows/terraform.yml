# This is a basic workflow for terraform 
name: terraform CI

on:
  push:
    branches:
     - github-action
  pull_request:
    branches:
      - github-action   
jobs:
  terraform_init:
    name: Terraform Init
    runs-on: ubuntu-latest
    steps:
      - name: terraform_init
        run: terraform init
    
     
     
